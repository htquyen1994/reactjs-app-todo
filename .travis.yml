sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t quyenht/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true quyenht/docker-react npm run test
